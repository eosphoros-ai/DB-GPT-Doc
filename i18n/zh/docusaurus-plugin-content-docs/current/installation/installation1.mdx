# 安装待办事项

DB-GPT 支持安装和使用各种开放源码和封闭源型号。 不同的模式对环境和资源有不同的要求。 如果需要本地化的模型部署，部署需要全球定位系统资源。 API 代理模型所需资源相对较少，可以在 CPU 机器上部署和启动。

:::info 备注

-   详细安装和部署教程可在 [Installation](/docs/installation1.mdx) 中找到。
-   此页面仅基于ChatGPT 代理和本地维库纳模型进行部署。
    :::

## 环境准备

### 下载源代码

## 测试

```python

```

:::info 备注

要了解更多配置内容，您可以查看 `docker-compose.yml` 文件
:::

### 测试

```python

```

:::info 备注

一些包含 _Markdown_ `语法` 的 **内容**。 看看[这个 `api`](#)。
:::

:::note

一些包含 _Markdown_ `语法` 的 **内容**。 看看[这个 `api`](#)。

:::

:::tip

一些包含 _Markdown_ `语法` 的 **内容**。 看看[这个 `api`](#)。

:::

:::警告

一些**内容** 和 _Markdown_ `syntax` 。 看看[这个 `api`](#)。
